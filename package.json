{
  "name": "floodlens-sri-lanka",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Sri Lanka flood and extreme-weather analytics — Vercel + GitHub Actions",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node tests/core.test.mjs && node tests/react.test.mjs",
    "check": "node --check src/app.js",
    "start": "python serve.py"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.25.5",
    "jsdom": "26.1.0"
  }
}
